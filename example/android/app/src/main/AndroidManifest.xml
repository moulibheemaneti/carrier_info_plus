<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      The plugin declares no permissions of its own, deliberately: READ_PHONE_STATE
      is a runtime permission that appears in the Play Console and obliges the app
      to file a data-safety declaration, so it is the app's call. This example
      declares both so it can demonstrate the full API.

      Without this line, Activity.requestPermissions() is denied immediately and
      silently -- no dialog is ever shown -- which looks exactly like a broken
      button.
    -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <!--
      Not a runtime permission and never prompted for. TelephonyManager.isDataEnabled()
      accepts this instead of READ_PHONE_STATE, so declaring it is what makes
      capabilities.isDataEnabled and network.cellularDataState answer at all.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:label="carrier_info_plus_example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
